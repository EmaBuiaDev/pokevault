# Dependencies
node_modules/
# package-lock.json is committed intentionally: catalog-ingest.yml (CI) uses
# `npm ci`, which requires it for reproducible, fast installs.
yarn.lock

# Wrangler & Build outputs
.wrangler/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
recompress-progress.ndjson
.recompress-tmp/
catalog-live.json
import-catalog.sql
.rarity-tmp/
